# Demo theme extension

A minimal renderer extension for the MyST book theme, built with [Module Federation](https://module-federation.io/).
It exists to demo runtime theme extensions in this documentation.

See the [Theme renderer extensions](../../extensions.md) docs page for how it works and how to run it locally.
